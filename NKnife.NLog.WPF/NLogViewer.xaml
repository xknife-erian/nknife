<UserControl x:Class="NKnife.NLog.WPF.NLogViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:self="clr-namespace:NKnife.NLog.WPF"
             mc:Ignorable="d"
             d:DesignHeight="230" d:DesignWidth="457"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <ListView ItemsSource="{Binding LogEntries}" Name="_LogView_">
            <ListView.ItemContainerStyle>
                <Style TargetType="{x:Type ListViewItem}">
                    <Setter Property="ToolTip" Value="{Binding ToolTip}" />
                    <Setter Property="Background" Value="{Binding Background}" />
                    <Setter Property="Foreground" Value="{Binding Foreground}" />
                    <Setter Property="FontFamily" Value="Consolas" />
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="LightYellow" />
                            <Setter Property="Foreground" Value="black" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{Binding BackgroundMouseOver}" />
                            <Setter Property="Foreground" Value="{Binding ForegroundMouseOver}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn x:Name="_TimeColumn_" DisplayMemberBinding="{Binding Time}"
                                        Header="{Binding RelativeSource={RelativeSource FindAncestor,AncestorType={x:Type self:NLogViewer}},Path=TimeHeader}"
                                        Width="{Binding RelativeSource={RelativeSource FindAncestor,AncestorType={x:Type self:NLogViewer}},Path=TimeWidth}" />
                        <GridViewColumn x:Name="_LevelColumn_" DisplayMemberBinding="{Binding Level}"
                                        Header="{Binding RelativeSource={RelativeSource FindAncestor,AncestorType={x:Type self:NLogViewer}},Path=LevelHeader}"
                                        Width="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type self:NLogViewer}}, Path=LevelWidth}" />
                        <GridViewColumn x:Name="_MsgColumn_" DisplayMemberBinding="{Binding FormattedMessage}"
                                        Header="{Binding RelativeSource={RelativeSource FindAncestor,AncestorType={x:Type self:NLogViewer}},Path=MessageHeader}"
                                        Width="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type self:NLogViewer}}, Path=MessageWidth}" />
                        <GridViewColumn x:Name="_NameColumn_" DisplayMemberBinding="{Binding LoggerName}"
                                        Header="{Binding RelativeSource={RelativeSource FindAncestor,AncestorType={x:Type self:NLogViewer}},Path=LoggerNameHeader}"
                                        Width="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type self:NLogViewer}}, Path=LoggerNameWidth}" />
                        <GridViewColumn x:Name="_ExColumn_" DisplayMemberBinding="{Binding Exception}"
                                        Header="{Binding RelativeSource={RelativeSource FindAncestor,AncestorType={x:Type self:NLogViewer}},Path=ExceptionHeader}"
                                        Width="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type self:NLogViewer}}, Path=ExceptionWidth}" />
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>