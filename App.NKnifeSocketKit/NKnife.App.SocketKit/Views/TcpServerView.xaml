<xcad:LayoutDocument x:Class="NKnife.App.SocketKit.Views.TcpServerView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:xcad="http://schemas.xceed.com/wpf/xaml/avalondock"
                     xmlns:resx="clr-namespace:NKnife.App.SocketKit.Properties"
                     mc:Ignorable="d">
    <Grid>
        <Grid.Background>
            <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.ControlLightColorKey}}" />
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="64" />
            <RowDefinition Height="*" />
            <RowDefinition Height="26" />
        </Grid.RowDefinitions>
        <ToolBar HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Row="0">
            <Button Click="Start">
                <Image Source="../Common/Res/start.png"></Image>
            </Button>
            <Button Click="Pause">
                <Image Source="../Common/Res/pause.png"></Image>
            </Button>
            <Button Click="Restart">
                <Image Source="../Common/Res/restart.png"></Image>
            </Button>
            <Button Click="Stop">
                <Image Source="../Common/Res/stop.png"></Image>
            </Button>
        </ToolBar>
        <ListView Width="Auto" Height="Auto" Grid.Row="1" />
    </Grid>
</xcad:LayoutDocument>