<xcad:LayoutDocument x:Class="NKnife.App.SocketKit.Views.LoggerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xcad="http://schemas.xceed.com/wpf/xaml/avalondock"
             mc:Ignorable="d"
             Title="日志管理器">
    <Grid>
        <ListView Name="_LoggerGrid"
                  IsSynchronizedWithCurrentItem="True" 
                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" SelectionMode="Single" 
                  Height="Auto" Width="Auto">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="时间" DisplayMemberBinding="{Binding Path=Time}" Width="90" />
                    <GridViewColumn Header="级别" DisplayMemberBinding="{Binding Path=Level}" Width="60" />
                    <GridViewColumn Header="日志" DisplayMemberBinding="{Binding Path=Message}" Width="Auto" />
                    <GridViewColumn Header="日志源" DisplayMemberBinding="{Binding Path=Source}" Width="420" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</xcad:LayoutDocument>